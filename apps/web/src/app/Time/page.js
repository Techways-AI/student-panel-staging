"use client";
import Time from '../../components/Time'; // Ensure the path and component name are correct

export default function TimePage() { // Ensure the function name matches the component
    return <Time />;
}

